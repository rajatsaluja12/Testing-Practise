package java_Programming;

/*
 * import java.util.ArrayList; import java.util.Arrays; import
 * java.util.LinkedHashSet; import java.util.List; import
 * java.util.stream.Collectors;
 * 
 * public class RemoveDuplicateElements {
 * 
 * public static void main(String[] args) {
 * 
 * ArrayList<Integer> numbers = new
 * ArrayList<Integer>(Arrays.asList(1,2,3,4,5,1,2,4,5,1,4));
 * 
 * //1. LinkedHashSet
 * 
 * LinkedHashSet<Integer> linkedhashset = new LinkedHashSet<Integer>(numbers);
 * ArrayList<Integer> numberswithoutDupElement = new
 * ArrayList<Integer>(linkedhashset);
 * System.out.println(numberswithoutDupElement); }
 * 
 * 
 * //2 JDK8 - Stream
 * 
 * ArrayList<Integer> markslist = new
 * ArrayList<Integer>(Arrays.asList(1,2,3,4,5,1,2,4,5,1,4,7,9,2)); 
 * List<Integer> marksunique = markslist.stream().distinct().collect(Collectors.toList());
 * System.out.println(marksunique);
 * 
 * 
 * } }
 */